# Docker Production Setup

Этот документ описывает настройку и развертывание приложения Everon Pro в production с использованием Docker.

## 📋 Требования

- Docker 20.10+
- Docker Compose 2.0+
- Минимум 2GB RAM
- Минимум 10GB свободного места

## 🚀 Быстрый старт

### 1. Подготовка окружения

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd everon-pro.dev

# Скопируйте файл окружения
cp env.example .env

# Отредактируйте .env файл с вашими настройками
nano .env
```

### 2. Настройка переменных окружения

Обязательно измените следующие переменные в `.env`:

```env
# Безопасные пароли для production
DB_PASSWORD=your_very_secure_password_here
DB_ROOT_PASSWORD=your_very_secure_root_password_here
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters

# Домен для production
DOMAIN=yourdomain.com
```

### 3. Запуск в development режиме

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### 4. Запуск в production режиме

```bash
# Использование готового скрипта развертывания
chmod +x scripts/deploy.sh
./scripts/deploy.sh

# Или ручной запуск
docker-compose -f docker-compose.prod.yml up -d
```

## 🔧 Управление

### Основные команды

```bash
# Запуск сервисов
docker-compose up -d

# Остановка сервисов
docker-compose down

# Перезапуск сервисов
docker-compose restart

# Просмотр логов
docker-compose logs -f [service_name]

# Выполнение команд в контейнере
docker-compose exec app sh
docker-compose exec mysql mysql -u root -p
```

### Миграции базы данных

```bash
# Запуск миграций
docker-compose run --rm migrate

# Или через профиль
docker-compose --profile migrate up migrate
```

### Резервное копирование

```bash
# Создание бэкапа базы данных
docker-compose --profile backup up backup

# Бэкапы сохраняются в папке ./backups/
```

## 📁 Структура файлов

```
├── Dockerfile                 # Docker образ приложения
├── docker-compose.yml         # Development конфигурация
├── docker-compose.prod.yml    # Production конфигурация
├── .dockerignore             # Исключения для Docker
├── env.example               # Пример переменных окружения
├── migrate.js                # Скрипт миграций
├── nginx/                    # Nginx конфигурация
│   ├── nginx.conf
│   └── conf.d/default.conf
└── scripts/                  # Скрипты управления
    ├── deploy.sh
    └── backup.sh
```

## 🔒 Безопасность

### SSL/HTTPS настройка

1. Получите SSL сертификаты (Let's Encrypt рекомендуется)
2. Поместите сертификаты в папку `./ssl/`:
   - `cert.pem` - сертификат
   - `key.pem` - приватный ключ
3. Раскомментируйте HTTPS секцию в `nginx/conf.d/default.conf`
4. Измените `server_name` на ваш домен

### Firewall настройки

```bash
# Открыть только необходимые порты
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 22/tcp  # SSH
ufw enable
```

## 📊 Мониторинг

### Health checks

- Приложение: `http://localhost/api/health`
- Nginx: `http://localhost/health`

### Логи

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f app
docker-compose logs -f mysql
docker-compose logs -f nginx
```

### Мониторинг ресурсов

```bash
# Использование ресурсов
docker stats

# Информация о контейнерах
docker-compose ps
```

## 🔄 Обновление

```bash
# Остановка сервисов
docker-compose down

# Обновление кода
git pull

# Пересборка и запуск
docker-compose build --no-cache
docker-compose up -d

# Запуск миграций (если есть)
docker-compose run --rm migrate
```

## 🐛 Troubleshooting

### Проблемы с подключением к БД

```bash
# Проверка статуса MySQL
docker-compose exec mysql mysqladmin ping -h localhost

# Подключение к MySQL
docker-compose exec mysql mysql -u root -p
```

### Проблемы с приложением

```bash
# Проверка логов приложения
docker-compose logs app

# Проверка переменных окружения
docker-compose exec app env | grep DB_
```

### Очистка

```bash
# Удаление всех контейнеров и образов
docker-compose down --rmi all --volumes --remove-orphans

# Очистка неиспользуемых ресурсов
docker system prune -a
```

## 📈 Production рекомендации

1. **Используйте внешнюю БД** для production
2. **Настройте мониторинг** (Prometheus + Grafana)
3. **Настройте логирование** (ELK Stack)
4. **Используйте CDN** для статических файлов
5. **Настройте автоматические бэкапы**
6. **Используйте secrets management** для паролей
7. **Настройте SSL** с автоматическим обновлением

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус контейнеров: `docker-compose ps`
3. Проверьте использование ресурсов: `docker stats`
4. Создайте issue с подробным описанием проблемы

